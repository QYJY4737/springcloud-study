#动态从springcloud-config中读取的配置文件
spring:
  profiles:
    active: native
  cloud:
    config:
      uri: http://192.168.10.101:10537
      name: application-zuul
      profile: dev
      #label:master #分支